#include <stdio.h>

int main() {
    int n; // ѕеременна€ дл€ хранени€ количества элементов
    printf("¬ведите количество элементов массива (N): ");
    scanf("%d", &n); // —читываем количество элементов

    int arr[n]; // ќбъ€вл€ем массив переменной длины (VLA)

    printf("¬ведите %d целых чисел:\n", n);
    // ÷икл дл€ ввода элементов
    for (int i = 0; i < n; i++) {
        printf("Ёлемент [%d]: ", i);
        scanf("%d", &arr[i]); // —читываем каждый элемент с клавиатуры
    }

    printf("\n¬ывод введенных элементов:\n");
    // ÷икл дл€ вывода элементов (дл€ проверки)
    for (int i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");

    return 0;
}
